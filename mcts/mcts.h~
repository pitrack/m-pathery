#pragma once
#include "../pathery/board.h"


// we can represent game states as a vector<int, int>

class MCTS {
 public:
  MCTS(Board b);
  void update();
  vector<pair<int, int> > get_sol();
  void run_game();
  
 private:
  
  

}
