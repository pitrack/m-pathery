#pragma once

enum TileType { Uninit, Empty, Start, End, FixedWall, FreeWall, Target };

class Tile {
 public:
  TileType getTileType();
  void changeTileType();
  Tile(TileType t);
  
 private:
  TileType t;  
};
