#pragma once
#include "tile.h"


class Board {
 public:
  int getHeight();
  int getWidth();
  Tile getTileAt(int x, int y);
  void putTileAt(int x, int y, Tile t);
  void print();
  Board(int width, int height);
  // other functions like clear board, remove, or copy might be desired.


 private:
  int width;
  int height;
  Tile** grid;

};
